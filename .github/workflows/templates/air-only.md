<!-- format-lint-check -->
:x: The formatting check failed. Please install [air](https://posit-dev.github.io/air/cli.html) and run the following command in the terminal (not the R console) to reformat the code:

```sh
air format .
```
