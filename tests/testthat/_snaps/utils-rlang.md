# check_class() handles columns with multiple classes

    Code
      check_class(dat, "x", "numeric")
    Condition
      Error:
      ! Column "x" in `dat` must be of class <numeric>, not <foo/bar/character>.

